metricsets: ["proxy"]
hosts:
{{#each hosts}}
  - {{this}}
{{/each}}
period: {{period}}

processors:
{{#if cluster_name}}
- add_fields:
    target: orchestrator.cluster
    fields:
      name: {{cluster_name}}
{{/if}}
{{#if processors}}
{{processors}}
{{/if}}
